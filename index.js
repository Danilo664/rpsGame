const rpsSigns = ["rock", "paper", "scissors"];
let signChoice = Math.floor(Math.random()*rpsSigns.length);
let sign = rpsSigns[signChoice];

function getComputerChoice(){
    return sign;
}







//alert(getComputerChoice())
